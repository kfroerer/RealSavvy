'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isBlank = isBlank;
exports.inGroupsOf = inGroupsOf;
exports.socialIconName = socialIconName;
function isBlank(value) {
  return value === undefined || value === null || value === '';
}

function inGroupsOf(array, number, callback) {
  var groups = [];
  var group = [];

  for (var i = 0; i < array.length; i++) {
    if (i % number === 0 && i > 0) {
      groups.push(callback(group.slice(0), groups.length));
      group = [];
    }
    group.push(array[i]);
  }

  if (group.length > 0) groups.push(callback(group.slice(0), groups.length));

  return groups;
}

function socialIconName(url) {
  if (url.match(/facebook\.com/)) return 'facebook';else if (url.match(/twitter\.com/)) return 'twitter';else if (url.match(/linkedin\.com/)) return 'linkedin';else if (url.match(/google\.com/)) return 'google-plus';else if (url.match(/youtube\.com/)) return 'youtube';else if (url.match(/skype:/)) return 'skype';else if (url.match(/\/feed\//)) return 'rss';else if (url.match(/pinterest\.com/)) return 'pinterest';else if (url.match(/instagram\.com/)) return 'instagram';

  return null;
}