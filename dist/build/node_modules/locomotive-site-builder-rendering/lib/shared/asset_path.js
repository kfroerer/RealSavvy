'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

exports.default = function (host, themeChecksum) {
  function buildPathFunction(path) {
    if (path === undefined || path === null) return '';

    var _path = path;

    if (!/^https?:\/\//.test(path)) {
      // Host
      if (host !== undefined) _path = '' + host + _path;

      // Checksum for theme assets
      if (/^\/(themes|site_builder)\//.test(path) && themeChecksum !== undefined) _path = _path + '?' + themeChecksum;
    }

    return _path;
  }

  return buildPathFunction;
};