// export default function()

// function buildRouteHandlerProxy(handlers) {
//   return function(context) {
//     console.log(context.themeHandle);
//     console.log(handlers);
//     // console.log(handlers[context.themeHandle])
//     return handlers[context.themeHandle](context);
//   };
// }

// function groupRoutes(themes) {
//   var routes = {}

//   Object.keys(themes).forEach((themeHandle) => {
//     const theme = themes[themeHandle];

//     if (theme.routes === undefined) return;

//     Object.keys(theme.routes).forEach((route) => {
//       const callRouteHandler = theme.routes[route];

//       if (routes[route] === undefined) routes[route] = {};

//       routes[route][themeHandle] = callRouteHandler;
//     });
//   });

//   // wrap handles inside a proxy function
//   Object.keys(routes).forEach((route) => {
//     routes[route] = buildRouteHandlerProxy(routes[route]);
//   });

//   return routes;
// }


// export default function(themes, callback) {
//   const routes = groupRoutes(themes);
//   Object.keys(routes).forEach((route) => {
//     callback(route, routes[route]);
//   });
// }
"use strict";